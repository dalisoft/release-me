feat(uWS): update something to latest version

fix(deps): upgrade dependencies to get latest security fixes

chore(lint): `lint-staged` config fix and upgrade but use temporarily hack

chore(format): use `biome` for faster and stable formatting

chore(lint): use latest `smartlint` for faster results

style(code): use proper import orders thanks to `biome`
